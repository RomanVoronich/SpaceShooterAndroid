android {
    signingConfigs {
        release {
            keyAlias 'dodge'
            keyPassword 'dodge1'
            storeFile file('/Users/sergey/Documents/Keys/dodge.jks')
            storePassword 'dodge1'
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }
}